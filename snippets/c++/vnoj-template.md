# Vietnam Online Judge Template (C++)

```c++
#include <bits/stdc++.h>

using namespace std;

void initialize(void) {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
}

int main(void) {
	initialize();
	//...
	return 0;
}
```